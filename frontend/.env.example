# URL du backend. Côté Vite, toute variable qui commence par VITE_ est exposée au bundle.
# En local, le backend tourne sur :3000 ou :3001 (compose). Laisser vide = fallback local.
VITE_API_URL=http://localhost:3000
